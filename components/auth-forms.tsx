'use client';
import {useState} from 'react';import {Button} from '@/components/ui/button';import {Input} from '@/components/ui/input';
export function SignOutButton(){const [error,setError]=useState('');return <><Button type="button" variant="outline" onClick={async()=>{try{const registration='serviceWorker' in navigator?await navigator.serviceWorker.getRegistration():null;const subscription=registration?await registration.pushManager?.getSubscription():null;const r=await fetch('/api/auth/logout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({endpoint:subscription?.endpoint})});if(!r.ok)throw Error('Unable to sign out. Please try again.');if(subscription)await subscription.unsubscribe().catch(()=>false);window.location.assign('/login')}catch(e){setError((e as Error).message)}}}>Sign out</Button>{error&&<span role="alert">{error}</span>}</>}
export function LoginForm(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password,next:new URLSearchParams(window.location.search).get('next')})});const data=await response.json();if(!response.ok)throw Error(data.error);setPassword('');window.location.assign(data.next)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <form onSubmit={submit} className="auth-form"><label>Email<Input type="email" autoComplete="username" required maxLength={254} value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<Input type="password" autoComplete="current-password" required maxLength={72} value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p role="alert" className="error">{error}</p>}<Button disabled={busy}>{busy?'Signing in…':'Sign in'}</Button><p>Your administrator provides your first password. You’ll choose a new password before accessing the app.</p><p>Forgot your password? Ask an administrator to issue a new temporary password.</p></form>}
export function ChangePasswordForm({email,mustChange}:{email:string;mustChange:boolean}){const [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[currentPassword,setCurrentPassword]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();if(password!==confirm){setError('The new passwords do not match.');return;}setBusy(true);setError('');try{const r=await fetch('/api/auth/change-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password,currentPassword})});const d=await r.json();if(!r.ok)throw Error(d.error);setPassword('');setCurrentPassword('');setConfirm('');window.location.assign(d.next)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <form onSubmit={submit} className="auth-form"><p>{email}</p>{mustChange?<p>Choose a new password to activate your account. Your temporary password has been used and cannot sign in again.</p>:<label>Current password<Input type="password" autoComplete="current-password" required maxLength={72} value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)}/></label>}<label>New password<Input type="password" autoComplete="new-password" required minLength={8} maxLength={72} value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm new password<Input type="password" autoComplete="new-password" required minLength={8} maxLength={72} value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><p>Use at least 8 characters. A memorable phrase works well. After changing your password, sign in and enable notifications again on your other devices.</p>{error&&<p className="error" role="alert">{error}</p>}<Button disabled={busy}>{busy?'Saving…':'Save password and continue'}</Button><SignOutButton/></form>}
