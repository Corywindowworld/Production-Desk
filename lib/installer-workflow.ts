import { z } from 'zod';
export const reportLabels={front:'Front of house',rear:'Rear of house',left:'Left side of house',right:'Right side of house',photos:'Job photos',issue:'Issue photos',completion:'Completion certificate / paperwork',incomplete:'Incomplete certificate',reorder:'Reorder form'} as const;
export const reportKinds=Object.keys(reportLabels) as (keyof typeof reportLabels)[];
export const reportAttachment=z.object({key:z.string().max(250),name:z.string().max(255),kind:z.enum(['front','rear','left','right','photos','issue','completion','incomplete','reorder'])});
export const reportSchema=z.object({id:z.string().uuid(),jobId:z.string().uuid(),version:z.number().int().min(1),status:z.enum(['Complete','Incomplete']),reason:z.string().trim().max(1000),notes:z.string().max(4000),installed:z.string().regex(/^\d{4}-\d{2}-\d{2}$/),attachments:z.array(reportAttachment).max(100)});
export function requiredReportKinds(status:string){return ['front','rear','left','right',...(status==='Incomplete'?['issue','incomplete','reorder']:['completion'])] as (keyof typeof reportLabels)[]}
export function reportError(r:{status:string;reason:string;attachments:{kind:string;key:string}[]}){if(r.status==='Incomplete'&&!r.reason.trim())return 'Explain why the job is incomplete.';for(const kind of requiredReportKinds(r.status))if(!r.attachments.some(a=>a.kind===kind))return `Add ${reportLabels[kind].toLowerCase()}.`;if(new Set(r.attachments.map(a=>a.key)).size!==r.attachments.length)return 'Use a separate upload for each required photo.';return ''}
export function installerJob(j:any){const {paymentMethod,id,number,customer,address,phone,specialNotes,notes,crew,supervisor,stage,install,installed,version,installerId,supervisorId}=j;return {paymentMethod:paymentMethod??null,id,number,customer,address,phone,specialNotes,notes,crew,supervisor,stage,install,installed,version,installerId,supervisorId}}
