import test from 'node:test';
import assert from 'node:assert/strict';
import {createServer} from 'vite';
test('operations preview aging boundaries and bonus rules',async()=>{const v=await createServer({configFile:false,server:{middlewareMode:true}});try{const {seed,aging,tier,quality,qualityTier}=await v.ssrLoadModule('/app/operations-preview/model.ts');const j=seed()[0];assert.equal(aging({...j,received:'2026-08-16'},'2026-09-15').aged,false);assert.equal(aging({...j,received:'2026-08-15',date:'2026-09-20'},'2026-09-15').aged,true);assert.equal(aging({...j,official:'INCOMP',inc:'2026-08-01'},'2026-09-15').aged,true);assert.equal(aging({...j,official:'PROD',started:'2026-09-08'},'2026-09-15').aged,false);assert.equal(aging({...j,official:'PROD',started:'2026-09-07',field:'Approved complete'},'2026-09-15').aged,true);assert.equal(aging({...j,official:'PROD',started:''},'2026-09-15').missing,true);assert.equal(seed().filter(j=>aging(j,'2026-09-15').aged).length,4);assert.equal(seed().filter(j=>aging(j,'2026-09-15').aged).reduce((n,j)=>n+(j.due??0),0),19200);assert.equal(tier(45735,'dollars'),1);assert.equal(tier(45736,'dollars'),.75);assert.equal(tier(0,'jobs'),1.2);assert.equal(quality('2026-09-15').score,3.875);assert.equal(quality('2026-08-26').included.length,0);assert.equal(qualityTier(3.55),null);}finally{await v.close()}});
